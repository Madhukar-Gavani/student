#include<iostream>
using namespace std;
int main()
{
	int number,lastNumber,firstNumber;
	cin>>number;
	lastNumber = number%10;
	do
	{
		firstNumber = number/10;
		number = firstNumber;
		
		if(firstNumber >= 1 && firstNumber <= 9)
		break;
	
	} while(firstNumber > 0);
	cout<<lastNumber + firstNumber;
	return 0;
